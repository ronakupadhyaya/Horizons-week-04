{{! YOUR CODE HERE }}
<div style="width:40%">
  {{#each restaurants}}
  <div class = "panel panel-info">
    <div class = "panel-heading">
      <a href="http://localhost:3000/restaurant?id={{this._id}}"><h3 style="display:inline" class="panel-title">{{this.name}}</h3></a>
      <br/>
    </div>
  </div>
  <a class="btn btn-default" href="http://localhost:3000/newreview?name={{this.name}}">Add review</a>
  <br/>
  <br/>
  {{/each}}
</div>

<a href="http://localhost:3000/restaurants/new" class="btn btn-default">Add restaurant</a>

<!--<script>
function getDistanceFromLatLonInKm(lat1,lon1,lat2,lon2) {
  var R = 6371; // Radius of the earth in km
  var dLat = deg2rad(lat2-lat1);  // deg2rad below
  var dLon = deg2rad(lon2-lon1);
  var a =
    Math.sin(dLat/2) * Math.sin(dLat/2) +
    Math.cos(deg2rad(lat1)) * Math.cos(deg2rad(lat2)) *
    Math.sin(dLon/2) * Math.sin(dLon/2)
    ;
  var c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
  var d = R * c; // Distance in km
  return d;
}

function getLocation(lat, long) {
  if (navigator.geolocation) {
   navigator.geolocation.getCurrentPosition(showPosition);
  } else {
   console.log("Geolocation is not supported by this browser.");
  }
}

function showPosition(position) {
  console.log(position.coords.latitude, position.coords.longitude);
}

$('.panel-info').each(function(index) {

});
getLocation();
</script>-->
<script src="http://code.jquery.com/jquery-latest.min.js" type="text/javascript"></script>
