{{! YOUR CODE HERE }}

<div style="display: flex; flex-direction: column; align-items: center">
	<h4>Users ({{nb_users}})</h4>
	<ul class="list-group">
		{{#each users}}
		<li class="list-group-item">
			<a href="/users/{{this._id}}">{{this}}</a>
		</li>
		{{/each}}
	</ul>
</div>